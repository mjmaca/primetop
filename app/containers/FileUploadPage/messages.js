/*
 * FileUploadPage Messages
 *
 * This contains all the text for the FileUploadPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FileUploadPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FileUploadPage container!',
  },
});
