/*
 * VerifyPage Messages
 *
 * This contains all the text for the VerifyPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VerifyPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VerifyPage container!',
  },
});
