/*
 * SupplierListPage Messages
 *
 * This contains all the text for the SupplierListPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SupplierListPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SupplierList container!',
  },
});
