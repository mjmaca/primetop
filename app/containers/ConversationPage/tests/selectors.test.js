// import { selectConversationPageDomain } from '../selectors';

describe('selectConversationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
