/*
 * ConversationPage Messages
 *
 * This contains all the text for the ConversationPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ConversationPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ConversationPage container!',
  },
});
