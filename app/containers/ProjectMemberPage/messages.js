/*
 * ProjectMemberPage Messages
 *
 * This contains all the text for the ProjectMemberPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProjectMemberPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProjectMemberPage container!',
  },
});
