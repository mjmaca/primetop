/*
 * CreateProjectModal Messages
 *
 * This contains all the text for the CreateProjectModal component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CreateProjectModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateProjectModal component!',
  },
});
