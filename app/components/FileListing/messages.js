/*
 * FileListing Messages
 *
 * This contains all the text for the FileListing component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FileListing';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FileListing component!',
  },
});
