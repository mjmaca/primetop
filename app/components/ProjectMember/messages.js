/*
 * ProjectMember Messages
 *
 * This contains all the text for the ProjectMember component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ProjectMember';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProjectMember component!',
  },
});
