/*
 * DailyTask Messages
 *
 * This contains all the text for the DailyTask component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DailyTask';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DailyTask component!',
  },
});
