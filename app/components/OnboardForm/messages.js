/*
 * OnboadForm Messages
 *
 * This contains all the text for the OnboadForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.OnboadForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OnboadForm component!',
  },
});
