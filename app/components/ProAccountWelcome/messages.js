/*
 * ProAccountWelcome Messages
 *
 * This contains all the text for the ProAccountWelcome component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ProAccountWelcome';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProAccountWelcome component!',
  },
});
